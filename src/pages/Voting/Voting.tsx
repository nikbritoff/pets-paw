import MainLayout from '../../layouts/MainLayout/MainLayout';
import styles from './Voting.module.scss';

const Voting = () => {
  return (
    <MainLayout>
      <h1 className={styles.header}>Voting</h1>
    </MainLayout>
  );
};

export default Voting;
